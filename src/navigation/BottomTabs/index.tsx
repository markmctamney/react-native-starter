import React from 'react';

import TABS from './tabs';
import BottomTabNavigator from '../../components/BottomTabNavigator';

const BottomTabs = () => <BottomTabNavigator tabs={TABS} />;

export default BottomTabs;
