import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  hermesText: { color: 'blue' },
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;
